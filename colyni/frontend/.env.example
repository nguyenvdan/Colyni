# Dev: leave unset — Vite proxies /api and /v1 to the Colyni backend (see vite.config.ts).

# Production static build: point at your Colyni API origin (no trailing slash).
# VITE_COLYNI_API_URL=https://api.yourdomain.com
